<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Crypto Exchange Rates</title>
        <style>
            body {background-color: powderblue;}
            h1   {color: rgb(6, 32, 44)}
            ul   {color: rgb(12, 62, 85)}
            .submit-forms-flexbox {
                display: flex;
                flex-direction: row;
            }
            .search-form {
                border: 2px dotted grey;
                margin: 20px;
                padding: 20px;
            }
            form {
                display: flex;
                flex-direction: column;
            }
            form input[type=submit] {
                margin-top: 10px;
            }

        </style>
    </head>
    {{> header}}
    <body>
        <div class = submit-forms-flexbox>
            <div class = search-form>
                <form action = "/home/codeSearch" method ="POST">
                    <input type = "text" placeholder = "currency code" name = "currCode" required>
                    <input type = "submit" value = "Search">
                </form>
            </div>
            <div class = search-form>
                <form action = "/home/countrySearch" method ="POST">
                    <input type = "text" placeholder = "country name" name = "countryName" required>
                    <input type = "submit" value = "Search">
                </form>
            </div>
        </div>
    </body>
    {{> footer}}
</html>